---
import { getCollection } from 'astro:content';
import Base from '../layouts/Base.astro';
import ProjectCard from '../components/ProjectCard.astro';
import RouteMap from '../components/RouteMap.astro';

const all = (await getCollection('work')).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf(),
);
const featured = all.filter((e) => e.data.featured).slice(0, 3);
const now = all.filter((e) => e.data.status !== 'shipped').slice(0, 3);
---

<Base>
  <section class="pt-10 sm:pt-16">
    <h1 class="station text-[36px] sm:text-[56px] leading-[1.05]">
      <span class="text-(--cobalt)">Five</span> internships in.<br />Still building on weekends.
    </h1>
    <p class="mt-6 max-w-[85ch] text-[19px] leading-relaxed text-(--ink-muted)">
      I'm Rohan. Five internships across RBC, Ontario Teachers', the U of T Innovation Hub, Dye
      and Durham, and Sun Life, where I'm now full time on the SunAdvantage team.
    </p>
    <p class="mt-4 max-w-[85ch] text-[19px] leading-relaxed text-(--ink-muted)">
      On the side I co-founded SmartSyllabus out of ICUBE, and most weekends I'm building some
      small tool to test an idea I couldn't argue my way to. Some of it works. What works is
      below.
    </p>
    <div class="mt-8 flex flex-wrap gap-4">
      <a href="/work" class="btn btn-primary text-[14px]">See the work</a>
      <a href="/about" class="btn btn-secondary text-[14px]">About me</a>
    </div>
  </section>

  <section class="mt-20">
    <div class="label flex items-center justify-between border-b-2 border-(--ink) pb-3 text-[15px] font-bold">
      <span class="text-(--ink)">The line so far</span>
      <span class="text-(--ink-faint)">2022 → now</span>
    </div>
    <RouteMap />
    <div class="label flex flex-wrap gap-x-5 gap-y-2 border-t-2 border-(--ink) pt-4 text-[14px]">
      <span class="bullet bullet-1 text-[12px] font-bold">Line 1 · Strategy</span>
      <span class="bullet bullet-2 text-[12px] font-bold">Line 2 · AI Tools</span>
      <span class="bullet bullet-3 text-[12px] font-bold">Line 3 · Startup</span>
    </div>
  </section>

  {
    now.length > 0 && (
      <section class="mt-20">
        <div class="label border-b-2 border-(--ink) pb-3 text-[15px] font-bold text-(--ink)">Building now</div>
        <ul class="mt-0">
          {now.map((e) => (
            <li class="border-b-2 border-(--rule-soft)">
              <a
                href={`/work/${e.id}`}
                class="flex flex-wrap items-baseline gap-x-4 gap-y-1 py-4 transition-colors hover:text-(--cobalt)"
              >
                <span class="text-[18px] font-bold text-(--ink)">{e.data.title}</span>
                <span class="label ml-auto text-[13px] text-(--ink-faint)">{e.data.status}</span>
              </a>
            </li>
          ))}
        </ul>
      </section>
    )
  }

  <section class="mt-20">
    <div class="label flex items-center justify-between border-b-2 border-(--ink) pb-3 text-[15px] font-bold">
      <span class="text-(--ink)">Featured</span>
      <a href="/work" class="transition-colors hover:text-(--cobalt) text-(--cobalt) font-bold">All work →</a>
    </div>
    <div class="mt-6 grid gap-5">
      {featured.map((entry) => <ProjectCard entry={entry} />)}
    </div>
  </section>
</Base>
